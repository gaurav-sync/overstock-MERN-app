const express = require('express');

// import controllers
const cartRouter = express.Router();

cartRouter.post('/', );

// cartRouter.get('/user/:userId', getBlogsByUserId)
// productRouter.get('/:id', getBlogById)
// productRouter.get('/', getBlogsPaginated)

module.exports = {cartRouter};